import 'package:flutter/material.dart';

final BoxDecoration selectedTabStyle = BoxDecoration(
    color: Color(0XFFF66C50).withOpacity(0.1),
    borderRadius: BorderRadius.circular(20.0)
);

final BoxDecoration defaultTabStyle = BoxDecoration(
    color: Colors.transparent,
    borderRadius: BorderRadius.circular(20.0)
);